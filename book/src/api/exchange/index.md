# Module: `lib`

> Source: `lib.rs`

Exchange abstraction layer.
Defines the [`Exchange`](trait::Exchange) trait and provides exchange-specific
implementations (Binance and Hyperliquid public WebSocket adapters).
